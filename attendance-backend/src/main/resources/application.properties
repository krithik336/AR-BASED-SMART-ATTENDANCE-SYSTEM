spring.application.name=attendance-backend

# --- Database (Postgres) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/attendance_db
spring.datasource.username=postgres
spring.datasource.password=yogesh17
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- File upload (for student photos) ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- JWT ---
# NOTE: change this secret before any real deployment. Must be 256+ bits for HS256.
app.jwt.secret=qwertyuiopasdfghjklzxcvbnm1qa2ws3ed4rf5tg6yh
app.jwt.expiration-ms=86400000

# --- Server ---
server.port=8080
